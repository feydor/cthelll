% map print_odd on range 0..100
debut:
    mov a,1
to100:
    mov b,a
    mod a,2
    beq a,0,even
    call print_odd
even:
    mov a,b
    add a,1
    bne a,100,to100
    beq a,100,fin
odd:
    # b
    ret
fin:
